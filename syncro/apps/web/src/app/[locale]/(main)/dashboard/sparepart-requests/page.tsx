import { SparepartRequestsPageContent } from "@/features/sparepart-requests/components/sparepart-requests-list";

/**
 * Sparepart requests route (story 12-1). Lists all requests scoped to the user's
 * plant/group — replaces the earlier placeholder.
 */
export default function SparepartRequestsPage() {
  return <SparepartRequestsPageContent />;
}
