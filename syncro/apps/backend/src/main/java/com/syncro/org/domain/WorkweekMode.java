package com.syncro.org.domain;

/**
 * Plant working calendar workweek mode (blueprint A12, story 15-2): values match the
 * {@code plant_working_calendars.workweek_mode} CHECK constraint in V1 exactly.
 */
public enum WorkweekMode {
  FIVE_DAY,
  SIX_DAY
}
