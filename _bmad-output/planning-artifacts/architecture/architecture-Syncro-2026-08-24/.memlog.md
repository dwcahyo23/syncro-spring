---
scope: Maintenance workorder/preventive/sparepart-request module + OPA authz + sync + org structure (lanjutan Epic 1-8)
purpose: Architecture spine untuk modul maintenance pada Syncro
altitude: initiative
updated: 2026-08-24T18:43
---

- (decision) Mode: Fast path - draft spine penuh dengan ASSUMPTION tags, user review. Deliverable: spine build only (untuk story/epic), tanpa rendering tambahan
- (constraint) Inherit Phase 1: modular monolith, bounded contexts com.syncro.<domain> dengan layer api/application/domain/infrastructure; controller->application service; repository module-local; ddl-auto=validate + Flyway (next V41); standard error shape; timestamps UTC; enum uppercase; traceId di semua; frontend hanya via Spring API (NFR-013a)
- (constraint) Inherit Phase 1: auth_app role SUPER_ADMIN/MANAGE/VIEWER + job ladder TECHNICIAN<STAFF<LEADER<SPV<MANAGER via machine_responsibilities(machine_id,user_id,level unique); plant scope via auth_user_plant_assignments; WAHA outbox (notification_jobs hardwire alert_id + escalation_level unique, notification_attempts); Garage object storage (key-only in PG); audit immutable; shift config V40 machine_group_shift_windows/machine_shift_windows
- (version) OPA 1.19.1 (current stable, dirilis 2026-08-17) - verified via GitHub releases
- (decision) AD: Paradigma = modular monolith + externalized authorization (OPA sidecar). Backend module baru: com.syncro.org, com.syncro.maintenance (workorder, preventive, sparepartrequest), com.syncro.authz (PolicyDecisionPoint), com.syncro.sync (external WO sync). Mencegah: authz logic tersebar, modul saling menyentuh repo
- (decision) AD: OPA sidecar HTTP REST, default-deny; input minimal self-contained (subject roles/plants/sections/machineGroups/teams + resource + action + context); org data TETAP di PostgreSQL, passed as input (bukan copy ke OPA data store); coarse allow di interceptor, row-level scope di query layer, per-resource decision di application service. Mencegah: stale org data di OPA, frontend jadi enforcement
- (decision) AD: Allowed-actions endpoint Spring /api/v1/authz/allowed-actions untuk frontend menu hiding; frontend tidak pernah panggil OPA langsung (NFR-013a); hiding = UX only, enforce server-side
- (decision) AD: WO dual source - id = external sheet_no (SYNCED) atau auto-gen WO-YYMMxxxx (INTERNAL, transaksi+lock anti duplikat); kolom source ENUM + sync_version; parent-child nullable; conflict: master field eksternal menang, operasional lokal menang. Mencegah: id collision, konflik sync vs lokal
- (decision) AD: State machines eksplisit (pola table-driven ala alert): WO lifecycle DRAFT->OPEN->ASSIGNED->IN_PROGRESS->ON_PROCUREMENT->IN_PROGRESS->DONE->CLOSED + CANCELLED; sparepart request REQUESTED->ACKED->PROCESSING->[READY
- (decision) AD: MTBF/MTTR dihitung backend dari WO (MTBF urut woStopAt antar breakdown; MTTR = kumulatif repair sessions duration); dipakai untuk dashboard/KPI. Mencegah: bug urutan id referensi, frontend hitung sendiri
- (decision) AD: Section leadership DERIVED dari machine_responsibilities level LEADER per machine group (bukan role manual); scope = machine group sendiri. Mencegah: duplikasi setup org vs responsibility
- (decision) AD: Sync module hardened: batch transaksi, idempotency by sheet_no, watermark resume, lock anti-konkurensi, urut sheet_no, quarantine failed rows, sync_runs audit, typed config, retry/backoff, timezone Asia/Jakarta->UTC, notifikasi dedupe. Mencegah: semua kelemahan referensi (no tx, no watermark, no lock, storm notif, bug ordering)
- (decision) AD: notification_jobs diperluas polymorphic target (target_type/target_id nullable; alert_id tetap untuk existing); WO/preventive/escalation pakai mekanisme outbox yang sama (rate limit + circuit breaker reuse Epic 5)
- (decision) AD: Evidence & technical drawing (before/after, PDF/JPEG/PNG/WebP, max 10MB) via Garage object storage; PG simpan object key hanya. Mencegah: bloat PG, bytes di DB
- (decision) AD: Stock OP/OQ terikat MATERIAL CODE (global 1 code = 1 sparepart), per plant dengan ref global; sparepart_stock(material_code, plant_id, stock_on_hand, order_point, order_qty). Reorder rule (stock<=OP -> PR qty=OQ) = business rule di aplikasi; aksi PR diotorisasi OPA
- (decision) AD: Preventive TANPA telemetry dependency - calendar/shift based (MONTHLY/ANNUAL, pakai shift config V40), floating interval dari completion. Lifetime counter hanya bila telemetry ada
- (decision) AD: Cross-plant team dengan expiry: scope ekstra terbatas, OPA input hanya sertakan team aktif (non-expired). Mencegah: akses permanen lintas plant
- (change) Reviewer fixes: AD-1 (OPA input single assembler + degraded-mode + decision-log masking/retention), AD-2 (scope dims = plant/machineGroup/team, single org scope service), AD-3 (id VARCHAR50, idempotencyKey, parent-child FOR UPDATE), AD-4 (derived transitions write history + parent close gate), AD-5 (derived history rows + ack clock dari history), AD-6 (analytics recompute + invalidation), AD-7 (sync via WorkorderImportService, mapping step), AD-8 (sync_field_mappings config + terminal-state protection), AD-9 (polymorphic idempotency key + alert_id nullable), AD-11 (stock owner inventory + optimistic lock + atomic update), AD-13 (teams merged in SQL scope)
- (change) AD-15 baru: role taxonomy migration (PRD 10 roles via additive V41 CHECK; MANAGE->MANAGER_MAINTENANCE, VIEWER->read-only; subject.roles = role + derived scope)
- (change) AD-16 baru: approval SoD (requester!=approver sebelum OPA; tier est cost; no-price -> section-leader)
- (change) Operational/Environmental envelope ditambahkan (OPA sidecar container, sync job @Scheduled, bundle CI, decision log retention, sidecar-down default-deny) + Open Questions OQ-1..OQ-7 dievakuasi dari PRD
- (change) Stack: Spring Boot 4.0.6->4.0.8, Next.js 16.2.6->>=16.2.11 (16.3) catatan security-review; Garage source dikoreksi
- (event) spine finalized - 16 AD, lint bersih, reviewer fixes applied
