---
title: 'Deferred-work bundle 10: alerts history type integrity — Orval regen, generated-type adoption, full attempt timeline'
type: 'chore'
created: '2026-08-23'
baseline_revision: 'f5ed328'
final_revision: 'a1e94e6'
status: 'done'
review_loop_iteration: 0
followup_review_recommended: false
context:
  - '_bmad-output/project-context.md'
  - '_bmad-output/implementation-artifacts/deferred-work.md'
warnings: ['multiple-goals']
---

<intent-contract>

## Intent

**Problem:** Three open deferred-work items around the alert notification history surface: (DW-84) `auditLogEntryViewEntityType.ts` had `ALERT` hand-added and would be deleted by the next `generate:api`; (DW-85) `alert-notification-history.tsx` mirrored backend DTOs (`NotificationAttemptView`, `NotificationJobView`, `AlertNotificationHistoryResponse`) as local interfaces pending that same regen; (DW-86) the attempts table renders only `.slice(0, 3)` rows — arbitrary truncation while maxAttempts is configurable.

**Approach:** Regenerate the API client against the live running backend (`npm run generate:api`) so `ALERT` is generator-produced (DW-84 verified: live `/v3/api-docs` serves the ALERT enum value). Adopt the generated types in `alert-notification-history.tsx`, deleting the local mirrors and normalizing optional fields once at the component boundary via `normalizeJob()` so render-tree non-null assumptions survive (DW-85). Remove `.slice(0, 3)` so every attempt renders (DW-86 frontend half; the backend ordering half — secondary `createdAt asc` sort in `findByAlertIdOrderByEscalationOrder` — already exists in code and is closed as already-resolved). Regen fallout fixed mechanically: renamed enum import (`SparepartAlertStatus` -> `AlertViewStatus`), response-type rename (`WahaTemplateViewResponse` -> `upsertWahaTemplateResponse`), optional-safe status pill label, stale-banner items guard.

## Boundaries & Constraints

**Always:**
- Generated files change ONLY via `npm run generate:api` output — no hand edits under `src/lib/api/generated/**`.
- Normalization happens once per history payload (`normalizeJob`); render components keep their existing field-access style.
- AttemptTable renders ALL attempts; expansion/copy behavior unchanged.
- Fallout fixes limited to type renames/guards required for compilation; no behavioral rewrites beyond the truncation removal.

**Block If:**
- Live backend unavailable or serving an OpenAPI spec without ALERT in AuditLogEntryView.entityType → HALT blocking condition `regen source incomplete`.

**Never:**
- Do NOT edit generated files by hand.
- Do NOT change escalation-order logic, status mapping, copy-to-clipboard, or audit-diff rendering.
- Do NOT reintroduce local DTO mirrors alongside the generated ones.

## Code Map

- `syncro/apps/web/src/lib/api/generated/**` -- regenerated by orval (ALERT now generated; SparepartAlertStatus model folded into AlertViewStatus union; notification DTOs now generated)
- `syncro/apps/web/src/features/alerts/alert-notification-history.tsx` -- generated-type adoption + normalizeJob + full attempt rendering -- DW-85/DW-86
- `syncro/apps/web/src/features/alerts/alert-detail-page-content.tsx` -- stale-banner optional-items guard -- fallout
- `syncro/apps/web/src/features/alerts/alert-status-badge.tsx`, `alert-list-page-content.tsx` -- enum rename fallout
- `syncro/apps/web/src/features/alerts/notification-state-pill.tsx` -- optional-status label fallback -- fallout
- `syncro/apps/web/src/features/waha-templates/waha-template-page-content.tsx` -- response type rename fallout

## Tasks & Acceptance

**Execution:**
- [x] Run `generate:api` against live backend; verify ALERT present without hand edits -- DW-84
- [x] `alert-notification-history.tsx` -- adopt generated types, delete local mirrors, add normalizeJob boundary -- DW-85
- [x] `alert-notification-history.tsx` -- remove slice(0,3); AttemptTable renders all attempts -- DW-86
- [x] Fallout: enum/response renames + optional guards across 5 consumer files

**Acceptance Criteria:**
- Given a fresh clone, when `generate:api` runs against the live backend, then `git diff` on `auditLogEntryViewEntityType.ts` is empty (ALERT survives regeneration).
- Given the notification history component, when rendering jobs, then all displayed fields derive from generated models with no local mirror interfaces.
- Given a job with more than three attempts, when the attempts table expands, then every attempt row renders.
- Given `tsc --noEmit`, vitest unit suite, and biome lint, when run, then all pass.

</intent-contract>

## Spec Change Log

_Empty until first review loopback._

## Review Triage Log

### 2026-08-23 — Review pass
- intent_gap: 0
- bad_spec: 0
- patch: 9 (low)
- defer: 1 (medium — orval input pinned to mutable live endpoint; committing an OpenAPI snapshot or pinning the spec source is a workflow decision for correct-course/sweep)
- reject: 14 (sentinel-fabrication philosophy — contract guarantees backend always writes these fields; whitespace-only-string truthiness absurd; attempt-cap reintroduction would undo DW-86 and DOM blow-up requires backend contract violation; unknown-status→OPEN amber default pre-existing badge behavior; type re-export from client module — no server importers exist; copyText dead fallback harmless; stale-banner guard wording nit in change summary, code correct; escalation-timeline dead null-render line pre-existing outside perimeter; audit-entry key fallbacks pre-existing untouched domain; raw-status undefined in banner checks fails conservative-safe; Invalid Date tooltip pre-existing path; generated union widening internal-only with rank-99 catch-all preserved deliberately; green-suite claim addressed by adding the two new component tests)
- addressed_findings:
  - `high` `patch` props alertStatus re-declared literal union drifting from generator -> aliased to generated AlertViewStatus
  - `high` `patch` history-table timestamp fallback used ?? so empty-sentinel sentAt blocked createdAt fallback, diverging from timeline -> both paths use ||-parity
  - `medium` `patch` sort comparator could return NaN on unparseable createdAt -> Number.isNaN guard returns 0
  - `high` `patch` tooltip interpolated raw optional status ("Notification: undefined") while badge showed UNKNOWN -> single UNKNOWN fallback for both surfaces
  - `medium` `patch` attempt rows keyed by possibly-colliding attemptNumber -> composite `${attemptNumber}-${index}` key with string expansion Set
  - `high` `patch` slice(0,3) removal and escalation ordering shipped without any test -> new component suite proving 4 attempts all render and TECHNICIAN sorts before STAFF regardless of input order

## Verification

**Commands:**
- `npm --prefix syncro/apps/web run generate:api && git diff --stat syncro/apps/web/src/lib/api/generated/model/auditLogEntryViewEntityType.ts` -- expected: empty diff for that file after regen
- `npx tsc --noEmit` (web) -- expected: zero errors
- `npm --prefix syncro/apps/web run test:unit` -- expected: green

## Auto Run Result

**Status:** done

**Summary:** Bundled three open deferred-work items (DW-84, DW-85, DW-86) as alerts-history type integrity: (DW-84) regenerated the Orval client against the live backend — pre-verified to serve ALERT in AuditLogEntryView.entityType — so `auditLogEntryViewEntityType.ts` is now generator-produced with an empty re-regen diff; (DW-85) deleted the hand-mirrored DTO interfaces in `alert-notification-history.tsx` and adopted generated types through a single boundary normalizer (`normalizeJob`) preserving render-tree non-null assumptions; regen fallout fixed mechanically across five consumer files (SparepartAlertStatus→AlertViewStatus rename, WahaTemplateViewResponse→upsertWahaTemplateResponse, optional-status guards); (DW-86) removed the arbitrary `.slice(0, 3)` attempts truncation and pinned full-timeline rendering plus escalation ordering with a new component test suite. The backend ordering half of DW-86 (secondary createdAt sort) was verified already present in `findByAlertIdOrderByEscalationOrder` and closed as already-resolved.

**Files changed:**
- `src/lib/api/generated/**` — regenerated by orval only (6 files; ALERT now generated; SparepartAlertStatus model folded away)
- `alert-notification-history.tsx` — generated-type adoption, normalizeJob boundary, full attempt rendering
- `alert-notification-history.test.tsx` — NEW: all-attempts + escalation-order tests
- `alert-detail-page-content.tsx`, `alert-status-badge.tsx`, `alert-list-page-content.tsx`, `notification-state-pill.tsx`, `waha-template-page-content.tsx` — mechanical fallout fixes

**Review findings breakdown:** 0 intent_gap, 0 bad_spec, 9 patches applied across 6 fix groups (props union alias, timestamp ||-parity, NaN-safe sort, tooltip UNKNOWN fallback, attempt composite keys, evidence tests), 1 deferral (OpenAPI snapshot pinning — workflow decision), 13 rejected.

**Follow-up review recommendation:** false — fallout fixes mechanical; behavioral deltas small and now test-pinned; suites green.

**Verification performed:**
- Pre-regen: live `/v3/api-docs` confirmed serving ALERT enum value
- Post-regen: `auditLogEntryViewEntityType.ts` empty diff vs hand-edited version
- `tsc --noEmit`: zero errors
- vitest unit suite: 183 passed / 7 skipped (skips pre-existing)
- biome lint on touched feature files: only pre-existing warnings

**Residual risks:** orval input remains the mutable live endpoint (deferred above) — a stale/mismatched running backend would silently reshape generated types; mitigation is reviewing generated-diff size per regen.
