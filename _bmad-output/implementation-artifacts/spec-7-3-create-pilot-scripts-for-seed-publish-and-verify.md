---
title: 'Create Pilot Scripts for Seed, Publish, and Verify'
type: 'feature'
created: '2026-08-22'
status: 'ready-for-dev'
review_loop_iteration: 0
followup_review_recommended: false
baseline_commit: 6205594
context: []
warnings: []
---

<intent-contract>

## Intent

**Problem:** Epic 7 must run the end-to-end pilot without manual guesswork, but today every step is a hand-copied command: the 7-1 seed header documents the hardened psql apply command, 7-2 delivers wire fixtures at `syncro/tests/fixtures/`, and the consumer contract (publish file body verbatim, refresh only `timestamp`, keep `messageId` verbatim) lives only inside the 7-2 spec prose. Stories 7-4/7-5/7-6/7-7 all need the same four repeatable actions (seed, publish before-threshold, publish threshold, verify evidence) and have no scripted, documented, Windows-PowerShell-native way to run them. Compounding this, the 3-13 EMQX security layer is deny-by-default and its seeded example device `device_BF-08410_GM1` is only allowed to publish to `factory/BF-08410/GM1/telemetry` — plant/machine INVERTED relative to the backend topic grammar (`TelemetryTopic.parse`: segment 1 = plantCode, segment 2 = machineCode), so no existing MQTT credential can publish the pilot payload to the canonical topic `factory/GM1/BF-08410/telemetry`, and the host has no MQTT client installed at all (`mosquitto_pub`/`mqttx` absent).

**Approach:** Four Windows PowerShell scripts at the architecture-pinned paths `syncro/scripts/seed-pilot.ps1`, `publish-jbf19-before-threshold.ps1`, `publish-jbf19-threshold.ps1`, `verify-pilot.ps1` (architecture.md directory tree names all four exactly; "Dev Script Expectations" lists seed / publish ×2 / verify telemetry / verify alert / verify WAHA job). `seed-pilot.ps1` wraps EXACTLY the 7-1 review-hardened apply flow (stdin pipe into `docker compose --env-file <env> -f syncro/infra/docker-compose.yml exec -T postgres sh -c 'PGCLIENTENCODING=UTF8 psql -v ON_ERROR_STOP=1 -U "$POSTGRES_USER" -d "$POSTGRES_DB"'` — in-container env expansion, atomic BEGIN/COMMIT) with the Windows-PowerShell 5.1 UTF-8 pipe guard that live testing proves is load-bearing for the `·` label (verified: normalized md5 identical WITH the guard, corrupted WITHOUT it). The publish scripts implement the 7-2 consumer contract through the EMQX Management API (verified live against the running EMQX 6.2.2): `POST /api/v5/login` with the dashboard credentials docker-compose already maps from `SYNCRO_MQTT_USERNAME`/`SYNCRO_MQTT_PASSWORD` → JWT bearer → `POST /api/v5/publish` (qos 1, retain false) with the fixture body after a regex refresh of only `timestamp` — zero host tooling, zero hardcoded secrets, all connection values from `syncro/.env`. `verify-pilot.ps1` checks pilot state through the same in-container psql pattern (seed rows, `machine_counter_states`, `sparepart_alerts`, `notification_jobs`, `telemetry_quarantine` diagnostics) plus `redis-cli` latest-hash evidence, interprets expectations from the current counter state, and prints operator/technical evidence pointers. One small infra correction accompanies the scripts: the 3-13 example device is corrected to the canonical `device_GM1_BF-08410` → `factory/GM1/BF-08410/telemetry` (CSV + entrypoint ACL + README) so the documented real-device path finally matches the backend grammar; the scripts themselves do not depend on it.

## Boundaries & Constraints

**Always:**
- Exact script paths fixed by the epics Story 7.3 AC and the architecture.md tree (lines 984-988): `syncro/scripts/seed-pilot.ps1`, `syncro/scripts/publish-jbf19-before-threshold.ps1`, `syncro/scripts/publish-jbf19-threshold.ps1`, `syncro/scripts/verify-pilot.ps1`. All four start with `$ErrorActionPreference = 'Stop'` and PowerShell comment-based help (`.SYNOPSIS`/`.DESCRIPTION`/`.EXAMPLE`/`.NOTES`) documenting prerequisites, idempotency/dedupe behavior, and Windows PowerShell 5.1+ compatibility — this satisfies "scripts are documented for Windows PowerShell".
- Every script resolves its own paths from `$PSScriptRoot` (script lives in `syncro/scripts/`, so the syncro root is `Split-Path -Parent $PSScriptRoot`) and accepts an `-EnvFile` parameter defaulting to `syncro/.env` — the same env file docker-compose consumes. No absolute paths, no CWD assumptions.
- `seed-pilot.ps1` wraps the 7-1 apply command VERBATIM (in-container `sh -c` so `$POSTGRES_USER`/`$POSTGRES_DB` expand inside the container from the compose environment; `PGCLIENTENCODING=UTF8`; `-v ON_ERROR_STOP=1`; the seed's own `BEGIN; ... COMMIT;` provides atomicity). Do NOT invent a different mechanism (no `psql -f` copy-in, no `docker compose cp`, no host-shell env sourcing). The one deliberate addition over the seed-header text: the script invokes `docker compose --env-file $EnvFile -f $ComposeFile exec ...` with absolute resolved paths (the `validate-syncro-baseline.ps1` precedent) so it works from any CWD — the header's bare `docker compose -f ...` form relies on CWD auto-discovering `syncro/.env`; the in-container `sh -c` payload itself stays byte-identical.
- UTF-8 pipe guard is mandatory in `seed-pilot.ps1`: `$OutputEncoding = New-Object System.Text.UTF8Encoding($false)` + `Get-Content -Raw -Encoding UTF8 $SeedFile` before piping into `docker compose exec -T`. Live evidence: Windows PowerShell 5.1's default `$OutputEncoding` (ASCII) mangles the seed's nine UTF-8 `·` bytes (normalized md5 mismatch `e14ef79f...` vs `6b6b9d9b...`); with the guard the piped content is byte-identical. Check `$LASTEXITCODE -eq 0` after the pipe and fail loudly otherwise.
- `seed-pilot.ps1` preflight: verify Flyway migrations are applied before seeding — `... exec -T postgres sh -c 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -t -A -c "SELECT count(*) FROM flyway_schema_history WHERE success;"'` (verified working in-container without a password — local trust; returns 30 on the current local DB). Zero rows or a psql error (relation does not exist) → terminate with actionable guidance: boot the backend once against the stack or run `mvn -f syncro/apps/backend/pom.xml flyway:migrate` (mirrors the seed header's Preconditions). Post-apply: print a canonical-row summary (counts for plant GM1, group Forming, machine BF-08410/ACTIVE, sparepart `BF-08410GM1ELEPLCWEC000`, installation 1000/0/90, 3 users, 3 responsibilities by level) via the same in-container psql pattern so re-runs visibly show `INSERT 0 0`-style stability.
- Publish scripts implement the 7-2 recorded consumer contract exactly: publish the fixture FILE BODY as the MQTT message body to `factory/GM1/BF-08410/telemetry`; refresh ONLY `timestamp` with the current UTC instant at publish time — `"(Get-Date).ToUniversalTime().ToString(\"yyyy-MM-dd'T'HH:mm:ss'Z'\", [System.Globalization.CultureInfo]::InvariantCulture)"` (ISO-8601, no fractional seconds — `Instant.parse`-compatible), applied as a targeted regex replace on the RAW file text (`'"timestamp"\s*:\s*"[^"]*"'` → `'"timestamp": "<now>"'`) so every other byte (especially `messageId` `pilot-jbf19-before-threshold-890` / `pilot-jbf19-threshold-900`) stays verbatim; no JSON re-serialization of the payload (PS 5.1 `ConvertTo-Json` would reformat/escape). A `-NoTimestampRefresh` switch publishes the file fully verbatim (7-2 documents stale-timestamp effects: historical-looking Influx point, skewed latency indicator — the switch exists for exact-duplicate evidence runs, not the default flow).
- Publish transport = EMQX Management API on the local broker (verified live on the running `emqx/emqx:6.2.2`): (1) `POST http://{SYNCRO_MQTT_HOST}:{SYNCRO_MQTT_DASHBOARD_PORT}/api/v5/login` with `{"username": SYNCRO_MQTT_USERNAME, "password": SYNCRO_MQTT_PASSWORD}` (docker-compose maps these exact env keys to `EMQX_DASHBOARD__DEFAULT_USERNAME/PASSWORD`) → `.token`; (2) `POST .../api/v5/publish` with `Authorization: Bearer <token>` and body `@{ topic=$Topic; payload=$bodyText; qos=1; retain=$false } | ConvertTo-Json`. QoS 1 matches the backend subscription (`MqttSubscriptionConfig` adapter `setQos(1)`); `retain` false (payload contract has no retained semantics). Dashboard basic auth does NOT work for POST (live-probed: 401 `BAD_API_KEY_OR_SECRET`) — the login/JWT flow is required. `emqx ctl` has NO publish subcommand in 6.2.2 (full 248-line help verified) — do not attempt it.
- Publish scripts load `syncro/.env` into the process environment using the `start-backend.ps1` parsing idiom (`Get-Content | ForEach-Object { if ($_ -match '^([^#=][^=]*)=(.*)$') { ... } }`) and read `SYNCRO_MQTT_HOST` (default `localhost`), `SYNCRO_MQTT_DASHBOARD_PORT` (default `18083`), `SYNCRO_MQTT_USERNAME`, `SYNCRO_MQTT_PASSWORD` (no defaults for credentials — missing values terminate with "check syncro/.env" guidance, satisfying "environment variables/config, not hardcoded secrets"). Pass `-UseBasicParsing` on web calls (no-op on PowerShell 7, avoids the IE first-run dialog on 5.1). Assert the publish response echoes the topic and contains the fixture's `messageId` (runtime guard against any encoding mangling), then print messageId, `counting`, and the refreshed timestamp.
- Both publish scripts print the follow-up contract: run `verify-pilot.ps1` next; republishing the same fixture within the Redis dedupe window (default PT30S, key `syncro:machine:{machineId}:telemetry:dedupe:{messageId}`) produces the `mqtt_telemetry_duplicate` log line — intentional evidence for 7-5's duplicate-publish AC; after the window, the alert-level non-RESOLVED dedup guard and the notification-job idempotency key keep a republish harmless.
- `verify-pilot.ps1` reads state ONLY through the established in-container exec patterns: `docker compose --env-file <env> -f syncro/infra/docker-compose.yml exec -T postgres sh -c 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -t -A -c "<sql>"'` and `... exec -T redis redis-cli --raw HGETALL syncro:machine:{machineId}:latest` (redis runs without auth in the local compose; both patterns live-verified). Resolve the machine UUID by natural key — `SELECT m.id FROM machines m JOIN plants p ON p.id = m.plant_id WHERE p.code = 'GM1' AND lower(m.code) = 'bf-08410'` — never assume the seed's fixed UUID (the seed adopts pre-existing case-variant rows).
- `verify-pilot.ps1` sections and verdict semantics (each line prints PASS / FAIL / INFO, script exits non-zero only on FAIL): (1) PREFLIGHT — postgres `SELECT 1` and redis `PING` hard-fail; EMQX `GET /api/v5/status` (public, no auth — live-verified 200) and backend `GET http://localhost:8080/api/v1/health` are warn-only (persisted evidence outlives broker/backend restarts). (2) SEED — canonical row counts per 7-1. (3) TELEMETRY — `machine_counter_states.counting`/`updated_at` for BF-08410 (this row is saved on EVERY accepted persist — `TelemetryPersistenceService.persist` calls `counterStateRepo.save` unconditionally after the latest write), Redis latest hash fields (`counting`, `countingDelta`, `receivedAt`, `traceId`) with the PT5M TTL caveat (absent hash ≠ missing telemetry; counter state is the durable authority), and `telemetry_quarantine` as a diagnostic (count > 0 → FAIL printing the latest `rejection_reason`/`rejection_field`/`topic` — a quarantined publish is the fastest root-cause path). (4) ALERT — interpret by counter state: `counting=900` expects exactly one non-RESOLVED `sparepart_alerts` row (status `OPEN`/`ACKNOWLEDGED`/`RESOLVED` lifecycle, `consumed_percentage_snapshot=90.00`, print `trace_id`); `counting=890` expects zero rows (PASS "no alert — correct before-threshold state"). (5) NOTIFICATION — `notification_jobs` for the alert: `escalation_level`, `status` (enum: PENDING, ROUTING_FAILED, SENT, EXHAUSTED, ESCALATED, CANCELLED, RATE_LIMITED), `recipient_phone`, `trace_id`, `error_detail`; placeholder numbers (`6281234567801/02/03`) make PENDING + attempt-failure evidence the expected live-WAHA outcome. (6) ACKNOWLEDGEMENT RESULT — alert status plus per-level job listing (post-ack, STAFF/LEADER jobs must not be SENT after the acknowledgement — 7-6 owns the action; this section reports the state). Finally print operator/technical pointers: Machine Hub `/dashboard/master-data/machines/BF-08410`, telemetry dashboard, alerts, system health (web dev server assumed at `http://localhost:3000`), pgAdmin explicitly labeled "local/dev evidence tool only, not a product feature", and the optional Influx query `docker compose exec -T influxdb influxdb3 query --token "$INFLUXDB3_ADMIN_TOKEN" --database syncro "SELECT * FROM telemetry ORDER BY time DESC LIMIT 5"` (measurement `telemetry` per `InfluxTelemetryWriter.toPoint`; CLI syntax live-verified).
- Infra correction (3-13 follow-up, required): `syncro/infra/emqx/etc/auth-bootstrap.csv` device row becomes `device_GM1_BF-08410,Device@Mqtt#2026!Dev,false` (same committed dev-placeholder password pattern as the row it replaces — DW-46 documents the accepted dev convention); `docker-entrypoint.sh` ACL block for the device upserts `{"username":"device_GM1_BF-08410","rules":[{"topic":"factory/GM1/BF-08410/telemetry","action":"publish","permission":"allow"}]}` and its comments/examples are corrected to "machine BF-08410 in plant GM1"; `infra/emqx/README.md` current-rules table gets the corrected device row AND the stale `syncro_backend → factory/#` entry corrected to `factory/+/+/telemetry` (the 3-13 review decision tightened it; the README was never updated). This follows the README's own documented convention `device_{plantCode}_{machineCode}` → `factory/{plantCode}/{machineCode}/telemetry`, which for the canonical pilot data (plant GM1, machine BF-08410 — PRD §12, seed) yields exactly the values above.
- The scripts must run on Windows PowerShell 5.1 (the AC's target — `powershell.exe` 5.1.26100 is the host default) and remain PowerShell 7 compatible: no `??`/`?.` operators, no PS7-only cmdlets; the UTF-8 guard makes 5.1 correct and is a no-op on PS7 defaults.

**Block If:** No decisions require human input. The publish mechanism was selected from live-verified options because no host MQTT client exists (`mosquitto_pub`/`mqttx` absent), `emqx ctl publish` does not exist in EMQX 6.2.2, and the Management API publish requires zero new host/container dependencies while satisfying the env-not-hardcoded-secrets AC. If the running EMQX ever disables the dashboard or the login endpoint, block and reassess (e.g., enabling an API key via env) — do not fall back to committing credentials.

**Never:**
- Never modify `pilot-seed.sql`, any Flyway migration, any Java production code, `application.yml`/`application-local.yml`, or anything under `syncro/apps/web/` — this story adds four scripts plus the three-file EMQX infra correction only.
- Never hardcode secrets in the scripts: no MQTT passwords, no JWT secrets, no Postgres credentials — connection values come from `syncro/.env` (or the documented non-secret defaults `localhost`/ports). The Postgres path needs no password at all (in-container `psql -U "$POSTGRES_USER"` uses container-local trust — the 7-1 hardened command's own mechanism).
- Never expand `$POSTGRES_USER`/`$POSTGRES_DB` on the host (the exact bug the 7-1 review fixed) or drop `ON_ERROR_STOP`/`PGCLIENTENCODING=UTF8` from the seed apply; never bypass the pipe-based stdin flow that the seed header pins.
- Never re-serialize, reorder, or reformat the fixture JSON (no `ConvertFrom-Json`/`ConvertTo-Json` round-trip of the payload) — the 7-2 verbatim-body contract and the `.gitattributes` LF pinning exist so the wire bytes match the committed fixture; only the `timestamp` regex substitution may touch the text.
- Never create `docs/pilot-validation.md` or `docs/screenshots/pilot/` (story 7-7), `tests/e2e/` specs (later stories), a `PilotFixture.java`/`pilot-fixture.json` shared helper (7-1/7-2 fenced: no second consumer yet — the scripts read the fixture files directly), or any new backend/frontend test — script-only verification plus the optional live smoke run is this story's evidence.
- Never have the scripts write business state themselves: they apply the (idempotent) seed, publish MQTT data, and READ evidence; acknowledgement, alert resolution, and WAHA session actions stay in the UI/API stories (7-5/7-6). No script may reset/wipe databases or Docker volumes.
- Never use the swapped topic `factory/BF-08410/GM1/telemetry` anywhere (the 3-13 example bug): the backend parses it as plant=BF-08410/machine=GM1 → `unknown_plant` quarantine; the canonical pilot topic is `factory/GM1/BF-08410/telemetry`.
- Never add `-Topic` free-form publishing of arbitrary payloads: the publish scripts accept the canonical fixture and canonical topic as defaults; if a `-Topic`/`-FixtureFile` override parameter exists, it is for diagnostics only and the script still prints exactly what it published.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Seed apply, fresh migrated DB | Flyway 30/30, seed not yet applied | All canonical rows created; summary counts printed; exit 0 | — |
| Seed re-run | Seed already applied (current local DB state) | Every statement `INSERT 0 0`; identical summary counts; exit 0 (idempotent — `WHERE NOT EXISTS` guards) | — |
| Seed apply without migrations | Empty DB / no `flyway_schema_history` | Preflight query errors or returns 0 | Terminate BEFORE piping SQL; print migration guidance (boot backend once / `mvn flyway:migrate`) |
| Windows PowerShell 5.1 pipe encoding | Seed piped with UTF-8 guard | Byte-identical content incl. nine `·` chars (live-verified md5 match) | Guard absent → `·` corrupted to `?` — spec mandates the guard; post-apply summary shows the correct label `Electric · PLC · Wecon · LX5` as a visible tripwire |
| Docker/stack down | `docker compose exec` fails | Non-zero exit, psql/pipe error surfaces | Terminate with "start the stack first" + the compose file path used |
| psql mid-file failure | `ON_ERROR_STOP=1` aborts inside `BEGIN/COMMIT` | Whole apply rolls back (atomic) | Script fails loudly on `$LASTEXITCODE` |
| Publish, backend subscribed | Before-threshold fixture, timestamp refreshed | EMQX 200 + echo; backend ingests: counter state 890, no alert; verify TELEMETRY PASS | — |
| Publish, backend stopped | Threshold fixture, mechanics check | EMQX 200 + echo (message accepted by broker; `cleanSession(true)` means the stopped backend never receives it — no delayed state surprise) | — |
| Duplicate publish inside PT30S | Same fixture re-run quickly, `messageId` verbatim | Broker accepts; backend logs `mqtt_telemetry_duplicate`, persist skipped — no second alert/job | Script header documents this as 7-5 evidence, not an error |
| Publish with stale `.env` dashboard creds | Login 401/4xx | `Invoke-RestMethod` throws (ErrorActionPreference Stop) | Catch and print: check `SYNCRO_MQTT_USERNAME`/`SYNCRO_MQTT_PASSWORD` in `syncro/.env` — they are the EMQX dashboard credentials docker-compose maps |
| Publish with backend on another host/port | `-EnvFile` pointing at custom env | All connection values from that env | Missing credential keys → terminate with guidance |
| Fixture drifted (7-2 key-set test failing) | Extra/renamed field in fixture JSON | Script still publishes the file verbatim (data-only transport) | `PilotMqttPayloadFixtureTest` is the drift gate, not the script; verify section surfaces downstream rejection if any |
| Verify before any publish | counter state absent, no alerts | TELEMETRY: counter row absent → INFO "no telemetry yet"; ALERT: expects none → PASS; exit 0 | Not an error — pre-publish baseline state |
| Verify after before-threshold only | `counting=890` | No alert expected (PASS); Redis hash may show 890 (if < 5 min old); exit 0 | — |
| Verify after threshold publish | `counting=900` | Exactly one non-RESOLVED alert (90.00 snapshot, traceId) + TECHNICIAN job row (PENDING/ROUTING_FAILED with placeholder numbers, or SENT with a live WAHA session) | ROUTING_FAILED/PENDING with placeholder recipients is documented expected evidence, printed with explanation |
| Verify with polluted quarantine | `telemetry_quarantine` rows exist | FAIL with latest `rejection_reason` + topic (e.g. `unknown_plant` from the swapped 3-13 topic) | Operator inspects `raw_payload`/`rejection_field` in pgAdmin (local/dev tool) |
| Redis latest hash expired (> PT5M) | Hash absent, counter state present | INFO "latest hash expired (TTL 5m) — counter state authoritative"; not a FAIL | — |
| Acknowledged pilot alert | Alert status ACKNOWLEDGED | ACK section shows status + job timeline; STAFF/LEADER not SENT after ack (7-6 scope reports, not enforces) | — |
| Canonical device credential on running stack | CSV edited, `docker compose restart emqx` | ACL rule upserts every start; the CREDENTIAL only loads on a fresh EMQX data volume (bootstrap_file is first-start-only per README) | Documented in README fix + spec; scripts do not depend on the device credential (Management API path) |
| Old swapped device rule lingering on a running broker | `device_BF-08410_GM1` still in the built-in DB after the CSV/entrypoint edit | Publishing with it to the old topic still works but backend quarantines as `unknown_plant`; harmless pilot noise | Optional cleanup via dashboard/API noted in README; fresh stacks get only the corrected rule |

</intent-contract>

## Code Map

**All paths relative to repo root; NEW unless marked EDIT.**

- `syncro/scripts/seed-pilot.ps1` -- NEW -- comment-based help + `param([string]$EnvFile, [string]$ComposeFile, [string]$SeedFile)` (defaults resolved from `Split-Path -Parent $PSScriptRoot`: `.env`, `infra\docker-compose.yml`, `apps\backend\src\main\resources\db\seed\pilot-seed.sql`). Flow: `$ErrorActionPreference='Stop'`; UTF-8 guard (`$OutputEncoding = New-Object System.Text.UTF8Encoding($false)`); preflight migration count (in-container psql, `WHERE success` — 30 expected, 0/error → terminate with guidance); apply via `Get-Content -Raw -Encoding UTF8 $SeedFile | docker compose --env-file $EnvFile -f $ComposeFile exec -T postgres sh -c 'PGCLIENTENCODING=UTF8 psql -v ON_ERROR_STOP=1 -U "$POSTGRES_USER" -d "$POSTGRES_DB"'` (check `$LASTEXITCODE`); post-apply summary counts (plants GM1, groups Forming, machine BF-08410 + status, sparepart `BF-08410GM1ELEPLCWEC000` + name, installation 1000/0/90, users ×3, responsibilities per level) printed as a table; exit 0.
- `syncro/scripts/publish-jbf19-before-threshold.ps1` -- NEW -- comment-based help + `param([string]$EnvFile, [string]$FixtureFile, [string]$Topic = 'factory/GM1/BF-08410/telemetry', [switch]$NoTimestampRefresh)`, fixture default `syncro\tests\fixtures\mqtt-jbf19-before-threshold-payload.json`. Flow: load `.env` into process env (start-backend.ps1 regex idiom); read fixture `-Raw -Encoding UTF8`; unless `-NoTimestampRefresh`, regex-replace only the `timestamp` value with the current UTC instant (`yyyy-MM-dd'T'HH:mm:ss'Z'`, invariant culture); `POST {host}:{SYNCRO_MQTT_DASHBOARD_PORT}/api/v5/login` (username/password from env; host default `localhost`, port default `18083`) → token; `POST /api/v5/publish` bearer-authenticated, body `@{topic;payload;qos=1;retain=$false} | ConvertTo-Json`, `-UseBasicParsing`; assert 200 echo contains topic and the fixture `messageId`; print messageId / `counting` / timestamp / topic; print next-step hints (verify-pilot.ps1, 30s dedupe-window note for duplicate evidence).
- `syncro/scripts/publish-jbf19-threshold.ps1` -- NEW -- identical structure, fixture default `mqtt-jbf19-threshold-payload.json`; header notes the expected outcome (90.00% → one OPEN alert + TECHNICIAN job — proven end-to-end by 7-5, not by publishing).
- `syncro/scripts/verify-pilot.ps1` -- NEW -- comment-based help + `param([string]$EnvFile, [string]$ComposeFile, [string]$BackendHealthUrl = 'http://localhost:8080/api/v1/health', [string]$WebUrl = 'http://localhost:3000')`. Internal helpers: `Invoke-PilotSql <sql>` (in-container psql `-t -A -c`, returns scalar text), PASS/FAIL/INFO printer with a fail counter. Sections per Boundaries (PREFLIGHT / SEED / TELEMETRY / ALERT / NOTIFICATION / ACKNOWLEDGEMENT RESULT) with state-keyed expectations (`counting=890` → no alert expected; `counting=900` → one non-RESOLVED alert expected; no counter row → pre-publish baseline). Machine UUID resolved by natural key (GM1 + `lower(code)='bf-08410'`). Redis via `... exec -T redis redis-cli --raw HGETALL syncro:machine:{id}:latest` (HGETALL output parsed into key/value pairs). Prints the evidence pointers block (UI routes, backend health URL, pgAdmin local/dev-only note, optional Influx `influxdb3 query` command with `--token "$INFLUXDB3_ADMIN_TOKEN"` expanded in-container). Exit 0 unless any FAIL.
- `syncro/infra/emqx/etc/auth-bootstrap.csv` -- EDIT -- replace the example device row with `device_GM1_BF-08410,Device@Mqtt#2026!Dev,false` (corrected plant/machine order; `syncro_backend` row unchanged).
- `syncro/infra/emqx/etc/docker-entrypoint.sh` -- EDIT -- the device ACL `curl` block: username `device_GM1_BF-08410`, topic `factory/GM1/BF-08410/telemetry` ("may publish ONLY to its own topic (canonical pilot device: machine BF-08410 in plant GM1)"); fix the inverted comment line ("device_GM1_BF-08410 for machine BF-08410 in plant GM1"); the syncro_backend block and all bootstrap/stale-key logic stay untouched.
- `syncro/infra/emqx/README.md` -- EDIT -- current-rules table: `syncro_backend | subscribe | factory/+/+/telemetry` (was stale `factory/#`) and `device_GM1_BF-08410 | publish | factory/GM1/BF-08410/telemetry` ("Canonical pilot device — machine BF-08410 in plant GM1"); add a one-line note that the bootstrap CSV credential only loads on first EMQX data-volume creation while ACL rules upsert on every container start (matches the file's existing "Adding a New Device" guidance).
- Read-only references (do not modify): `syncro/apps/backend/src/main/resources/db/seed/pilot-seed.sql` (the wrapped flow + canonical values + counter math), `syncro/tests/fixtures/mqtt-jbf19-*.json` + `.gitattributes` (7-2 wire fixtures, LF-pinned), `syncro/infra/docker-compose.yml` (env keys, port mappings, dashboard credential mapping lines 73-77), `syncro/.env.example` (env key contract for scripts), `syncro/scripts/start-backend.ps1` (env-file parsing idiom), `syncro/scripts/validate-syncro-baseline.ps1` (`docker compose --env-file` precedent), `application-local.yml` (env key names), `TelemetryTopic.java` (topic grammar: segment 1 plant, segment 2 machine), `MqttSubscriptionConfig.java` (QoS 1 subscription), `TelemetryPersistenceService.java` (counter-state save, dedupe key, latest hash), `V19`/`V20`/`V24`/`V9` migrations (alert / counter-state / notification-job / quarantine schemas), `NotificationJobStatus.java` + `SparepartAlertStatus.java` (enum values printed), specs 7-1/7-2 (seed contract, fixture consumer contract).

## Tasks & Acceptance

**Execution:**

- [ ] `syncro/scripts/seed-pilot.ps1` -- NEW -- per Code Map: exact wrap of the 7-1 hardened apply command, UTF-8 pipe guard, migration preflight, post-apply summary. [AC 7.3-1, AC 7.3-5, AC 7.3-6]
- [ ] `syncro/scripts/publish-jbf19-before-threshold.ps1` -- NEW -- per Code Map: .env-driven EMQX login + `/api/v5/publish`, verbatim body with only-`timestamp` refresh (default on, `-NoTimestampRefresh` switch), messageId/counting printout, dedupe-window hint. [AC 7.3-2, AC 7.3-5, AC 7.3-6]
- [ ] `syncro/scripts/publish-jbf19-threshold.ps1` -- NEW -- same structure for the threshold fixture. [AC 7.3-3, AC 7.3-5, AC 7.3-6]
- [ ] `syncro/scripts/verify-pilot.ps1` -- NEW -- per Code Map: six evidence sections with state-keyed verdicts, in-container psql/redis reads, quarantine diagnostics, evidence pointers. [AC 7.3-4, AC 7.3-5, AC 7.3-6]
- [ ] EMQX infra correction: `auth-bootstrap.csv` device row, `docker-entrypoint.sh` ACL block + comments, `README.md` current-rules table + first-start note — canonical `device_GM1_BF-08410` → `factory/GM1/BF-08410/telemetry`. [supports AC 7.3-2/7.3-3: the documented device path matches the canonical topic; scripts themselves use the Management API and are unaffected]
- [ ] Verify (live, local stack): run `seed-pilot.ps1` TWICE → second run shows identical summary counts (idempotent, current DB already seeded); run `publish-jbf19-before-threshold.ps1` with the backend running → `verify-pilot.ps1` shows TELEMETRY PASS (counting 890, no alert) and exit 0; run `publish-jbf19-threshold.ps1` with the backend STOPPED → 200 echo proves mechanics without creating alert/job state that belongs to 7-5 (backend `cleanSession(true)` means no delayed delivery); confirm `syncro/tests/fixtures/.gitattributes`-pinned fixture bytes round-trip (publish echo contains the exact `messageId`). State exact checks run in the completion record. [all ACs]
- [ ] Verify scope: `git status --short` shows exactly the four new scripts + three edited infra files + story/sprint artifacts; zero changes under `syncro/apps/`, `db/seed/`, `db/migration/`, or frontend. [all ACs]

**Acceptance Criteria:**

- Given local infra is available (stack up, Flyway applied), when `seed-pilot.ps1` runs, then it applies the canonical pilot seed through the exact 7-1 hardened in-container psql flow (ON_ERROR_STOP, UTF-8 client encoding, atomic BEGIN/COMMIT), refuses to run before migrations exist, and prints the canonical-row summary — re-running it changes nothing (idempotent). [AC 7.3-1]
- Given the pilot seed exists, when `publish-jbf19-before-threshold.ps1` runs against the local stack, then the 7-2 fixture's file body is published verbatim (only `timestamp` refreshed, `messageId` verbatim) to EMQX topic `factory/GM1/BF-08410/telemetry` at QoS 1, using only env-file connection values, and a subscribed backend accepts it as counting=890 telemetry. [AC 7.3-2]
- Given the before-threshold publish succeeded, when `publish-jbf19-threshold.ps1` runs, then the threshold fixture is published the same way to the same topic (counting=900; alert/notification outcomes are proven by 7-5, not asserted by the script). [AC 7.3-3]
- Given pilot publishes have run (any stage), when `verify-pilot.ps1` runs, then it checks and reports: seed rows, accepted telemetry (counter state + Redis latest + quarantine diagnostics), alert state (state-keyed: none at 890, one non-RESOLVED at 900 with 90.00 snapshot and traceId), notification job state per escalation level, and acknowledgement result (alert status + per-level job timeline), exiting non-zero only on genuine failures and printing operator/technical evidence pointers (UI routes, pgAdmin local/dev-only note, optional Influx query). [AC 7.3-4]
- Given any of the four scripts, every connection value (env file path, hosts, ports, credentials, URLs) comes from parameters, `syncro/.env`, or documented non-secret defaults (localhost/ports) — no secret is ever hardcoded, and the PostgreSQL path needs no credential at all (in-container trust). [AC 7.3-5]
- Given a Windows developer machine, each script carries PowerShell comment-based help (synopsis, prerequisites, examples, PowerShell 5.1+ compatibility notes) and runs successfully under Windows PowerShell 5.1 (`powershell.exe`) — the UTF-8 pipe guard preserving the seed's `·` characters is the explicit 5.1-correctness proof. [AC 7.3-6]

### Review Findings

(review fills this section)

## Spec Change Log

- 2026-08-22: Spec created (draft → ready-for-dev). Ultimate context engine analysis completed — comprehensive developer guide created.

## Design Notes

- **Why the Management API publish and not an MQTT client:** the host has no MQTT client (`mosquitto_pub`/`mqttx` absent — checked), `emqx ctl` in EMQX 6.2.2 has no publish subcommand (full help enumerated), and pulling a helper container/image or requiring a host install would violate the no-new-tools rule and add fragility. The Management API path was verified live against the running broker: `/api/v5/status` is public; dashboard-basic-auth POSTs fail with 401 `BAD_API_KEY_OR_SECRET`; `POST /api/v5/login` with the compose-mapped dashboard credentials returns a JWT; `POST /api/v5/publish` with the bearer token reaches real validation (`topic_name_invalid` on empty topic, qos validation error) — endpoint and auth confirmed without publishing anything. This path is pure `Invoke-RestMethod`, env-driven, and exercise-neutral: the backend subscriber receives management-API publishes exactly like device publishes (same topic/QoS-1 delivery), so 7-4/7-5 evidence is unaffected. The real-device path remains available and is what the corrected `device_GM1_BF-08410` example now documents — for manual parity testing with any MQTT client an operator chooses to install.
- **Why the seed wrap must stay byte-exact:** the 7-1 review hardened the apply command specifically to (a) expand `$POSTGRES_USER`/`$POSTGRES_DB` INSIDE the container (host expansion reads nothing), (b) abort atomically via `ON_ERROR_STOP` + the file's `BEGIN/COMMIT`, and (c) force UTF-8 for the `·` label. The only Windows-specific hazard left is the PowerShell pipe encoding, which this spec closes with the live-tested guard: WITHOUT it, Windows PowerShell 5.1 pipes ASCII and corrupts the seed's nine `·` characters (18 UTF-8 bytes across 3 lines — normalized md5 `6b6b9d9b...` vs correct `e14ef79f...`); WITH it the piped content is byte-identical. The post-apply summary doubling as a visible label check gives the operator a tripwire even if the guard is ever removed.
- **The 3-13 ordering bug and why this story fixes it:** `TelemetryTopic.parse` reads `factory/{plantCode}/{machineCode}/telemetry` (segment 1 = plant, segment 2 = machine — source-verified), and the canonical pilot data is plant `GM1` / machine `BF-08410`. The 3-13 example (`device_BF-08410_GM1` → `factory/BF-08410/GM1/telemetry`, README "machine GM1 in plant BF-08410") inverts both, so following the README against the pilot data publishes to a topic the backend rejects as `unknown_plant` (quarantined). The fix follows 3-13's own documented convention `device_{plantCode}_{machineCode}` → `factory/{plantCode}/{machineCode}/telemetry`, which for the pilot yields `device_GM1_BF-08410` → `factory/GM1/BF-08410/telemetry`. Kept small (CSV row + entrypoint block + README rows) and decoupled from the scripts: bootstrap CSV credentials load only on first data-volume creation (README-documented EMQX behavior), while ACL rules upsert on every container start — so a running stack gets the corrected rule on restart but keeps the old credential until manually added/removed; fresh stacks get both. The Management-API scripts work regardless, which is why they don't gate on it.
- **Evidence model of `verify-pilot.ps1`:** every check reads state the production pipeline already persists — `machine_counter_states` (saved unconditionally on each accepted persist), `sparepart_alerts` (V19, partial unique index `WHERE status != 'RESOLVED'` backs the "exactly one active alert" expectation), `notification_jobs` (V24, `UNIQUE (alert_id, escalation_level)` backs the no-duplicate-job expectation), `telemetry_quarantine` (fastest failure root-cause), and the Redis latest hash (5-minute TTL — treated as best-effort, with the durable counter state as authority). Expectations are keyed to the observed counter value (890 → no alert; 900 → one alert) so the script is correct at every pilot stage instead of assuming 7-5 already ran. Placeholder WhatsApp numbers mean PENDING/ROUTING_FAILED-with-attempt-evidence is the expected live-WAHA outcome until real numbers are set (seed header caveat) — reported as evidence, not failure.
- **Why threshold publish is verified backend-stopped:** a live threshold publish with the backend running would create the alert + TECHNICIAN job that story 7-5 must prove from a known state; with `cleanSession(true)` (3-1 config, DW-23/DW-61 documented) a stopped backend simply never receives the message, so the mechanics check (login → publish → 200 echo) is safe. The before-threshold publish IS run end-to-end here: counting=890 telemetry with no alert is precisely 7-4's invariant, and re-publishing in 7-4 is harmless (dedupe window expired; counter math unaffected).
- **Scope fences inherited from 7-1/7-2:** no `docs/pilot-validation.md` (7-7 will reference these scripts), no `tests/e2e/` specs, no `PilotFixture.java`/`pilot-fixture.json` (still no second code consumer — the scripts read the committed fixture files directly, keeping `syncro/tests/fixtures/` the single source of wire truth), no changes to seed/fixtures/production code. The scripts are deliberately "boring": plain PowerShell, docker compose exec patterns already established by the 7-1 apply command and `validate-syncro-baseline.ps1`, and web calls with the `start-backend.ps1` env idiom.
- **Grounding note (web research):** none required — every technical fact is pinned to repo source or a live probe executed at spec time (EMQX 6.2.2 endpoints/auth behavior, `emqx ctl` capabilities, PowerShell 5.1 pipe encoding md5 evidence, in-container psql/redis-cli/influxdb3 CLI syntax, running-stack status, local DB state: Flyway 30/30, seed applied, alerts table empty). No new libraries, versions, or external APIs are introduced.

## Verification

**Commands:**
- `powershell -NoProfile -File syncro/scripts/seed-pilot.ps1` (twice; from any CWD) -- expected: migration preflight passes (30), apply succeeds (`INSERT 0 0` family on re-run), canonical summary identical both times, exit 0.
- `powershell -NoProfile -File syncro/scripts/publish-jbf19-before-threshold.ps1` (backend running) -- expected: 200 publish echo with topic `factory/GM1/BF-08410/telemetry` and messageId `pilot-jbf19-before-threshold-890`, refreshed UTC timestamp printed.
- `powershell -NoProfile -File syncro/scripts/publish-jbf19-threshold.ps1` (backend stopped) -- expected: same mechanics proof for `pilot-jbf19-threshold-900` without creating alert/job state.
- `powershell -NoProfile -File syncro/scripts/verify-pilot.ps1` -- expected after the above: SEED PASS, TELEMETRY PASS (counter 890, no alert), NOTIFICATION/ACK sections report pre-alert baseline, exit 0.
- `git status --short` -- expected: exactly `syncro/scripts/seed-pilot.ps1`, `syncro/scripts/publish-jbf19-before-threshold.ps1`, `syncro/scripts/publish-jbf19-threshold.ps1`, `syncro/scripts/verify-pilot.ps1` (new), `syncro/infra/emqx/etc/auth-bootstrap.csv`, `syncro/infra/emqx/etc/docker-entrypoint.sh`, `syncro/infra/emqx/README.md` (edited), plus story/sprint artifacts.

**Manual checks (optional, no tooling):**
- Inspect each script's comment-based help renders (`Get-Help .\syncro\scripts\verify-pilot.ps1`).
- After a threshold publish with backend up (defer to 7-5 unless accepted), verify-pilot's ALERT section shows one OPEN alert with `consumed_percentage_snapshot=90.00` and the NOTIFICATION section a TECHNICIAN job; duplicate threshold publish inside 30s shows `mqtt_telemetry_duplicate` in backend logs and no second active alert/job.

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
